schema: maintain-architecture-decisions
version: 3
checks:
  package-contract:
    argv:
      - "{python}"
      - -m
      - unittest
      - tests.test_package.PackageStructureTests
  adr-tool-contract:
    argv:
      - "{python}"
      - -m
      - unittest
      - tests.test_adr.AdrSchemaContractTests
